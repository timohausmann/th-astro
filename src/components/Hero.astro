---
import HeroCanvas from "./HeroCanvas.astro";
---

<section class="hero">
    <HeroCanvas />
</section>

<style>
    .hero {
        position: fixed;
        inset: 0;
        z-index: 0;
        background: linear-gradient(to bottom, #fff 0%, #eee 100%);
        overflow: hidden;
    }
</style>
